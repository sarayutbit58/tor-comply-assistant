# TOR Comply Assistant

MVP for Thai government TOR compliance (checklist → annotate → Word export).

- `web/` — Next.js shell (phone + notebook)
- `contracts/` — OpenAPI v1.1
- `requirements.seed.json` — กพบ section seed
- Sample TOR/comply binaries are omitted from this public repo (`samples/` stays local)
